# Terraform
infra/.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
infra/.terraform.lock.hcl
crash.log

# Secret overrides (if any) — regular tfvars are tracked
*.secret.tfvars

# Distribution tarballs (large binaries)
infra/dist/

# OS
.DS_Store
